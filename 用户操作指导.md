# IAAdvisor2 API连接问题解决指导

## 问题描述
应用在登录时出现61秒超时错误，无法正常连接到后端服务 `https://ia-legal-advisor.onrender.com`

## 已实施的解决方案

### 1. 网络配置优化
- **增加了超时时间**：开发环境从60秒增加到120秒，特殊情况下180秒
- **实现服务器预热**：在发送验证码前会先预热服务器，减少冷启动时间
- **智能重试机制**：检测到冷启动时会自动重试，使用指数退避策略

### 2. 错误处理改进
- **新增冷启动检测**：当请求超时且耗时超过45秒时，识别为服务器冷启动
- **用户友好的错误信息**：
  - 服务器冷启动：显示"服务器正在启动，请稍候重试"
  - 网络超时：显示具体的超时原因和建议
  - 网络连接问题：提供详细的网络检查指导

### 3. 用户界面增强
- **网络状态横幅**：实时显示网络连接状态和服务器状态
- **加载进度指示**：冷启动时显示预计等待时间（1-2分钟）
- **错误状态图标**：不同类型错误显示不同的图标提示

### 4. 网络测试工具
新增了网络测试页面，可以：
- 测试基础网络连通性
- 检测服务器响应时间
- 验证API端点可用性
- 生成详细的测试报告

## 用户操作步骤

### 方法1：直接尝试登录（推荐）
1. 打开应用
2. 输入手机号
3. 点击"发送验证码"
4. **耐心等待**：首次启动可能需要1-2分钟
5. 如果看到"服务器正在启动"提示，请等待直到收到验证码
6. 输入验证码登录

### 方法2：使用网络测试工具
1. 在应用中找到"网络测试"页面（如果可用）
2. 点击"开始测试"
3. 等待所有测试完成
4. 查看测试结果：
   - ✅ 绿色表示正常
   - ❌ 红色表示有问题
   - ⚠️ 橙色表示响应慢但正常
5. 查看"测试报告"了解详细信息

### 方法3：手动检查网络
1. 确保设备已连接到网络（WiFi或移动数据）
2. 尝试用浏览器访问 https://ia-legal-advisor.onrender.com
3. 如果页面加载很慢或超时，说明是服务器冷启动问题
4. 等待几分钟后再次尝试

## 常见错误及解决方案

### "请求超时，可能是网络问题或服务器繁忙"
**原因**：服务器冷启动或网络连接问题
**解决方案**：
1. 等待1-2分钟后重试
2. 检查网络连接
3. 尝试切换WiFi/移动数据

### "服务器正在启动，请稍候重试"
**原因**：Render免费服务冷启动（正常现象）
**解决方案**：
1. 耐心等待，不要频繁重试
2. 首次启动可能需要1-2分钟
3. 后续使用会很快

### "网络连接不可用"
**原因**：设备网络连接问题
**解决方案**：
1. 检查WiFi/移动数据连接
2. 尝试打开其他网站测试网络
3. 重启网络连接

### "验证码错误或已过期"
**原因**：验证码输入错误或已过期
**解决方案**：
1. 检查验证码是否输入正确
2. 如果过期，重新获取验证码
3. 确保在5分钟内使用验证码

## 技术细节（开发者参考）

### 已优化的配置
```swift
// 超时配置
static var apiTimeout: TimeInterval {
    return 120.0 // 开发环境2分钟
}

static var longApiTimeout: TimeInterval {
    return 180.0 // 特殊情况3分钟
}
```

### 服务器预热机制
```swift
func warmupServer() async {
    // 预先调用health端点唤醒服务器
    let url = baseURL.appendingPathComponent("health")
    // ... 预热逻辑
}
```

### 冷启动检测
```swift
private func detectColdStart(error: Error, duration: TimeInterval) -> APIError {
    if let urlError = error as? URLError, 
       urlError.code == .timedOut, 
       duration > 45.0 {
        return APIError.serverColdStart
    }
    // ...
}
```

## 注意事项

1. **首次使用**：第一次登录时响应可能较慢，这是正常现象
2. **频繁重试**：不要在短时间内频繁重试，这可能导致更长的等待时间
3. **网络环境**：确保网络连接稳定，避免在网络信号弱的环境下使用
4. **耐心等待**：冷启动是免费服务的特性，请耐心等待

## 联系支持

如果按照上述步骤仍无法解决问题，请联系技术支持，并提供：
1. 错误信息截图
2. 网络测试报告
3. 设备型号和iOS版本
4. 网络环境描述（WiFi/移动数据/运营商）

---
*最后更新：2025-08-08*
*版本：v1.0*